// Buttons
const buttons = {
	PRIMARY: 0,
	SECONDARY: 1,
	TERTIARY: 2,
};

// Regular expressions
const regexps = {
	CMR_PHONE_NUMBER: /^(\+237|00237|\(237\))?((6|2)[5-9][0-9]{7})$/,
};

export { buttons, regexps };
